# Grammar

The supported COBOL grammar has not been defined yet.

Future grammar rules will describe only the explicitly supported subset of
COBOL and will evolve alongside lexer, parser, AST, and generator tests.
